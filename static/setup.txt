plugin.tx_newscalendar_pi1 = USER
plugin.tx_newscalendar_pi1 {

	file.cssCalendar = {$plugin.tx_newscalendar_pi1.file.cssCalendar}
	file.cssContextMenu = {$plugin.tx_newscalendar_pi1.file.cssContextMenu}
	file.listView.listViewTemplate = {$plugin.tx_newscalendar_pi1.file.listView.listViewTemplate}

	header.strftime = %B 
	listView.header.strftime = %A %e
	calendar.contextHeader.strftime = %A, %Y-%m-%d
	calendar.contextItem.strftime = %H:%M

	# Pagebrowser settings

	pageBrowser {
	pageFloat = CENTER
	limit = 3
	maxPages = 3
	showResultCount = 1
	order = DESC
	showPBrowserText = 0
	dontLinkActivePage = 1
	tableParams = cellpadding="2" align="center"
	
	# settings for the extended pageBrowser from TYPO3 3.8.0
	showFirstLast = 0
	pagefloat = CENTER
	showRange = 0
	disabledLinkWrap = <span style="color:#bbb;">|</span>
	inactiveLinkWrap = |
	activeLinkWrap = <strong>|</strong>
	LinksWrap =|
	showResultsWrap = |<br />
	showResultsNumbersWrap = 
	browseBoxWrap =  
	browseLinksWrap = |
	}

}